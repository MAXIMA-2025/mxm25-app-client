name: CI

on:
  push:
    branches:
      - main
      - staging
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # Example build step
      - name: Run tests
        run: echo "Running tests..." && sleep 3

      - name: Send status to Discord
        if: always()
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK_URL }}
          STATUS: ${{ job.status }}
          COMMIT_MESSAGE: ${{ github.event.head_commit.message }}
          PR_TITLE: ${{ github.event.pull_request.title }}
        run: |
          if [ "$STATUS" == "success" ]; then
            COLOR=3066993 # green
          elif [ "$STATUS" == "failure" ]; then
            COLOR=15158332 # red
          else
            COLOR=8359053 # yellow/other
          fi

          # pick commit message or PR title
          if [ -n "$COMMIT_MESSAGE" ]; then
            MESSAGE="$COMMIT_MESSAGE"
          elif [ -n "$PR_TITLE" ]; then
            MESSAGE="$PR_TITLE"
          else
            MESSAGE="No commit message found"
          fi

          curl -H "Content-Type: application/json" \
          -X POST \
          -d '{
            "embeds": [{
              "title": "🔔 GitHub Actions Status",
              "description": "Workflow **'"$GITHUB_WORKFLOW"'** for `'"$GITHUB_REPOSITORY"'` on branch `'"$GITHUB_REF_NAME"'` **'"$STATUS"'**",
              "url": "'"$GITHUB_SERVER_URL"'/'"$GITHUB_REPOSITORY"'/actions/runs/'"$GITHUB_RUN_ID"'",
              "color": '"$COLOR"',
              "fields": [
                {
                  "name": "Commit",
                  "value": "'"$GITHUB_SHA"'"
                },
                {
                  "name": "Message",
                  "value": "'"$MESSAGE"'"
                },
                {
                  "name": "Actor",
                  "value": "'"$GITHUB_ACTOR"'"
                }
              ],
              "timestamp": "'"$(date --utc +%Y-%m-%dT%H:%M:%SZ)"'"
            }]
          }' \
          $DISCORD_WEBHOOK
